{
  "name": "wp-boilerplate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node bin/sync.js --watch & vite",
    "build": "node bin/sync.js --production && vite build",
    "sync": "node bin/sync.js",
    "setup": "bash bin/setup.sh"
  },
  "devDependencies": {
    "chokidar": "^4.0.0",
    "dotenv": "^16.4.0",
    "sass": "^1.80.0",
    "vite": "^6.0.0",
    "vite-plugin-live-reload": "^3.0.0"
  }
}
